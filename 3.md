# 仙剑 RP 平台 · 最终融合设计

> **融合自**: 1.md (原始架构) + 2.md (技术能力) + 你的产品概念 (World/周目/GM/OC)
>
> **核心简化**: GM 不再是 first-class 实体,**就是一段 system_prompt 文本**
>
> **核心扩展**: OC 关系模板、内置 NPC 不可变、周目 (cycle) 替代 save_slot

---

## 0. 顶层心智模型(产品视角)

```
┌────────────────────────────────────────────────────────────────────┐
│                                                                    │
│   World (世界)                ← 顶层主体,玩家选择"进哪个世界"       │
│      │                                                             │
│      ├── 内置 NPC (性格不可变)    ← 世界的一部分,固定                 │
│      │    ├─ 李逍遥                                                  │
│      │    ├─ 林月如                                                  │
│      │    └─ 赵灵儿                                                  │
│      │                                                             │
│      ├── GM (文风 system_prompt) ← 世界可有多个 GM 模板              │
│      │    ├─ 林月如文风 (细腻情感)                                    │
│      │    └─ 古龙武侠 (凌厉短句)                                     │
│      │                                                             │
│      └── Cycle (周目)            ← 同世界可玩多次,每次独立           │
│           ├─ Cycle 1: 扮演李逍遥,GM 林月如文风                       │
│           ├─ Cycle 2: 扮演林月如,GM 古龙武侠                         │
│           └─ Cycle 3: 扮演 OC "师兄",GM 诙谐吐槽                     │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

**一句话**: **"选世界 → 选角色(内置/OC) → 选 GM → 开周目 → 玩"**

---

## 1. 核心概念精确定义

### 1.1 World(世界)

```
worlds/{world_id}/
├── setting.md              世界观设定(背景/历史/规则)
├── map.json                地图(地点/区域/连接)
├── npcs/                   内置 NPC 目录(性格不可变)
│   ├── li_xiaoyao.md       李逍遥
│   ├── lin_yueru.md        林月如
│   ├── zhao_linger.md      赵灵儿
│   └── npc_innkeeper.md    客栈老板
├── lorebook.json           世界书(关键词触发的世界知识)
├── rules.json              规则(骰子/数值/判定)
└── gms/                    该世界的 GM 模板(可选)
    ├── style_linyueru.md   林月如文风
    └── style_gulong.md     古龙武侠
```

**World 由作者创建**(可以是官方世界,也可以是用户自定义世界)。

**World 与 Cycle 的关系**: 1 个 World 可有 N 个 Cycle(周目),每个 Cycle 是独立的游玩过程。

### 1.2 内置 NPC(性格不可变)

```
worlds/{world_id}/npcs/{npc_id}.md
```

**关键约束**:
- 性格由 World 作者定义,所有 Cycle 共享
- 玩家**不能修改**内置 NPC 的性格
- 玩家**可以扮演**内置 NPC(source='npc')
- 玩家扮演内置 NPC 时,persona_data 来自 npc_base,**锁死不可改**

### 1.3 OC(自定义角色,Original Character)

玩家创建的**不属于任何 World 内置 NPC** 的角色。

**关键能力**:
- 自由定义 name/background/personality
- 创建时**必须选 OC 关系模板**(对每个内置 NPC 选一个)
- 跨 World 可复用(同一 OC 可在不同 World 用)
- persona_data **可自由编辑**

### 1.4 GM(文风 system_prompt)

**精确定义**(用户原话): **"GM 就是核心引擎层拼接一个控制文风的 system_prompt,能被更改的只有控制文风的 system_prompt"**

```
worlds/{world_id}/gms/{gm_id}.md  或  gms/{gm_id}.md (全局)
```

**存储**: **纯 markdown 文件**,内容就是一段 system_prompt 文本。

**例: gms/style_linyueru.md**:
```markdown
# 林月如文风

你是一位细腻情感向的叙事者:
- 句子长度中等(15-30 字为主)
- 大量使用通感和环境描写烘托情绪
- 角色对话带轻微娇嗔和试探
- 战斗场景以"心理压力"和"权衡"为主,弱化数值
- 遇到 NPC 之间情感拉扯时,优先铺陈内心独白
- 避免"金光闪闪""法力无边"等过度华丽用词
```

**例: gms/style_gulong.md**:
```markdown
# 古龙武侠文风

你是一位古龙风格的叙事者:
- 短句为主,单句不超过 15 字
- 大量留白和省略号"……"
- 战斗场景极简,重点在氛围和心理
- 角色对话机锋、警句、悬念
- 善用"你看着他,他也看着你"式对视
- 主角常带宿命感:"他知道自己躲不过"
```

**GM 的拼接机制**(GM 引擎层):

```rust
// engine/src/orchestrator/prompt_assembler.rs
pub fn build_system_prompt(cycle: &Cycle, gm: &GmPrompt) -> String {
    let mut parts = vec![];

    // 1. 角色人设(来自 perspective, 不可变或自定义)
    parts.push(format!("# 角色\n{}", cycle.perspective.persona_data));

    // 2. 世界观(来自 world)
    parts.push(format!("# 世界观\n{}", cycle.world.setting));

    // 3. GM 文风 system_prompt ← 唯一用户可改的"风格层"
    parts.push(format!("# 文风指引\n{}", gm.content));

    // 4. 当前状态(从 SQLite 投影)
    parts.push(format!("# 当前状态\n{}", cycle.current_state));

    // 5. 弧光上下文
    parts.push(format!("# 弧光\n{}", cycle.arc_context));

    // 6. 记忆上下文(检索结果)
    parts.push(format!("# 相关记忆\n{}", cycle.relevant_memories));

    // 7. 规则约束(来自 world.rules)
    parts.push(format!("# 规则\n{}", cycle.world.rules));

    parts.join("\n\n---\n\n")
}
```

**GM 的产品语义**:
- 玩家说"换 GM"= 选一个不同的 .md 文件
- 切换 GM **不影响 cycle 数据**,只影响后续生成的文风
- GM 是**全局可复用**的(同 GM 可用于仙剑/古龙/金庸等多个 world)

### 1.5 Perspective(用户扮演视角)

```sql
source = 'npc' | 'oc'
```

| 来源 | 含义 | persona_data 来源 | 可编辑 |
|---|---|---|---|
| **source='npc'** | 扮演内置 NPC | npc_base(锁死) | ❌ |
| **source='oc'** | 自定义角色 | 用户输入 | ✅ |

**Perspective 跨 World 复用**:
- "我是李逍遥"perspective 可在仙剑/仙剑2/仙剑3 等多个 world 用
- "我是 OC 师兄"perspective 可在任意 world 用

### 1.6 Cycle(周目)

**周目 = SillyTavern 的 chat 概念,但产品语义更清晰**。

```
同一 World 多次游玩 → 多个独立 Cycle
  ├─ 每次选不同 Perspective(我扮演谁)
  ├─ 每次选不同 GM(什么文风)
  └─ 每次事件/记忆/弧光/关系完全独立
```

**示例**:
```
仙剑奇侠传一
├── Cycle 1: 我是李逍遥,GM 林月如文风 → 走主线剧情
├── Cycle 2: 我是林月如,GM 古龙武侠 → 看李逍遥视角的反转
└── Cycle 3: 我是 OC 师兄,GM 诙谐吐槽 → 戏说江湖
```

### 1.7 OC 关系模板(pre-game setup)

OC 创建时,玩家**必须为每个内置 NPC 选一个关系模板**。

| 模板 | 初始 affinity | 初始 trust | 初始 intimacy | initial_type |
|---|---:|---:|---:|---|
| 萍水相逢 (tpl_stranger) | 0 | 0 | 0 | stranger |
| 一见钟情 (tpl_love_at_first_sight) | 70 | 30 | 50 | lover |
| 青梅竹马 (tpl_childhood_friend) | 60 | 80 | 70 | close_friend |
| 宿敌 (tpl_sworn_enemy) | -80 | 10 | 0 | enemy |
| 师徒 (tpl_master_student) | 40 | 90 | 50 | master |
| 命中注定 (tpl_destined) | 50 | 50 | 50 | destined |
| 无关系 (tpl_no_relation) | — | — | — | — |

**模板的作用**: 开周目时,自动用模板初始化 npc_user_relationships。

---

## 2. 完整 Schema(融合版)

### 2.1 文件存储(MCP-Server 托管)

```
data/                              # AIRP-MCP-Server 数据根
├── worlds/                        # 世界
│   └── xiatian_qixia_1/
│       ├── setting.md
│       ├── map.json
│       ├── rules.json
│       ├── lorebook.json
│       ├── npcs/
│       │   ├── li_xiaoyao.md
│       │   ├── lin_yueru.md
│       │   ├── zhao_linger.md
│       │   └── npc_innkeeper.md
│       └── gms/                   # 该世界专属 GM
│           └── style_linyueru.md
├── gms/                           # 跨世界通用 GM
│   ├── style_gulong.md
│   ├── style_horror.md
│   └── style_xxxx.md
├── perspectives/                  # 用户视角(全局,跨 world 复用)
│   ├── pov_npc_li_xiaoyao.json    # source='npc'
│   ├── pov_npc_lin_yueru.json
│   └── pov_oc_shixiong.json       # source='oc'
└── oc_relationship_templates.json  # 全局模板
```

### 2.2 SQLite 核心表(单 memory.db)

```sql
-- ===== A. 元数据 =====

CREATE TABLE meta (
  key TEXT PRIMARY KEY,
  value TEXT
);
INSERT INTO meta(key, value) VALUES('schema_version', '3');
INSERT INTO meta(key, value) VALUES('product_version', '1.0');

-- ===== B. 周目(Cycle,替代 save_slot) =====

CREATE TABLE cycles (
  cycle_id TEXT PRIMARY KEY,         -- 'cycle_xxx_001'
  world_id TEXT NOT NULL,            -- 'xiatian_qixia_1'
  perspective_id TEXT NOT NULL,      -- 'pov_npc_li_xiaoyao'
  gm_id TEXT NOT NULL,               -- 'style_linyueru' (file reference)
  cycle_name TEXT NOT NULL,          -- '周目 1: 李逍遥线-林月如文风'
  cycle_description TEXT,
  -- 状态
  current_world_time TEXT,           -- '景天元年 三月初七 午时'
  current_location_id TEXT,
  total_events INTEGER DEFAULT 0,
  total_playtime_seconds INTEGER DEFAULT 0,
  -- 元信息
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  last_played_at TEXT
);

CREATE INDEX idx_cycles_world ON cycles(world_id);
CREATE INDEX idx_cycles_updated ON cycles(updated_at DESC);

-- ===== C. 内置 NPC 当前状态(以 cycle 隔离) =====

CREATE TABLE character_states (
  cycle_id TEXT NOT NULL,
  npc_id TEXT NOT NULL,              -- 'lin_yueru'
  -- 弧光(以当前 perspective 视角下 NPC 的状态)
  arcs TEXT NOT NULL DEFAULT '{}',   -- JSON: {trust_user: 50, affection_user: 80, courage: 70, ...}
  -- 物理状态
  hp INTEGER, hp_max INTEGER,
  mp INTEGER, mp_max INTEGER,
  location_id TEXT,
  status_flags TEXT,                 -- JSON: {poisoned: true, ...}
  -- 弧光阶段
  arc_phase TEXT,                    -- 'stranger' | 'friend' | 'lover' | ...
  -- 心理状态
  perceived_mood TEXT,               -- 'happy' | 'sad' | 'angry' | 'neutral'
  last_event_id INTEGER,
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  PRIMARY KEY(cycle_id, npc_id)
);

-- ===== D. NPC↔User 关系(以 cycle 隔离) =====

CREATE TABLE npc_user_relationships (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cycle_id TEXT NOT NULL,
  npc_id TEXT NOT NULL,              -- 'lin_yueru'
  -- 关系度量
  affinity INTEGER DEFAULT 0,        -100~100
  trust INTEGER DEFAULT 0,           0~100
  intimacy INTEGER DEFAULT 0,        0~100
  respect INTEGER DEFAULT 0,         0~100
  current_type TEXT,                 -- 'stranger' | 'friend' | 'close_friend' | 'lover' | ...
  -- 来源(创建 cycle 时从模板初始化)
  initial_template TEXT,             -- 'tpl_childhood_friend'
  initial_setup_event_id INTEGER,    -- 关联 setup event
  -- 当前动态
  first_met_at TEXT,                 -- 游戏内首次相遇时间
  last_changed_event_id INTEGER,
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE(cycle_id, npc_id)
);

CREATE INDEX idx_rels_npc ON npc_user_relationships(cycle_id, npc_id);

-- 关系变更历史
CREATE TABLE relationship_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cycle_id TEXT NOT NULL,
  npc_id TEXT NOT NULL,
  field TEXT NOT NULL,               -- 'affinity' | 'trust' | 'intimacy' | 'respect' | 'type'
  old_value TEXT,
  new_value TEXT,
  delta INTEGER,
  event_id INTEGER,
  world_time TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX idx_rel_hist ON relationship_history(cycle_id, npc_id, created_at DESC);

-- ===== E. 事件溯源(单一真相源) =====

CREATE TABLE domain_events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cycle_id TEXT NOT NULL,            -- 周目隔离
  event_type TEXT NOT NULL,
  --   'setup' | 'arc_increment' | 'dialogue' | 'combat' | 'discovery' |
  --   'travel' | 'relationship_change' | 'state_change' | 'item_change' |
  --   'time_advance' | 'system'
  actor_id TEXT,
  actor_type TEXT,                   -- 'npc' | 'user' | 'system'
  target_id TEXT,
  target_type TEXT,                  -- 'npc' | 'user' | 'item' | 'location'
  location_id TEXT,
  arc_dimension TEXT,                -- 'trust_user' | 'affection_user' | 'courage' | ...
  delta REAL,
  world_time TEXT,
  importance REAL DEFAULT 0.5,
  summary TEXT,
  payload TEXT,                      -- JSON
  is_milestone INTEGER DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX idx_events_cycle_time ON domain_events(cycle_id, world_time DESC);
CREATE INDEX idx_events_cycle_actor ON domain_events(cycle_id, actor_id);
CREATE INDEX idx_events_cycle_target ON domain_events(cycle_id, target_id);
CREATE INDEX idx_events_cycle_type ON domain_events(cycle_id, event_type);
CREATE INDEX idx_events_milestone ON domain_events(cycle_id, is_milestone) WHERE is_milestone = 1;

-- ===== F. 三层记忆 =====

-- F.1 情景记忆(episodic)
CREATE TABLE episodic_memories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cycle_id TEXT NOT NULL,
  character_id TEXT NOT NULL,        -- 谁记得
  event_id INTEGER NOT NULL,         -- 关联事件
  content TEXT NOT NULL,
  emotional_valence REAL,            -1~1
  vividness REAL DEFAULT 1.0,        -- 0-1, 衰减
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  last_accessed_at TEXT,
  access_count INTEGER DEFAULT 0
);

-- F.2 语义记忆(semantic)
CREATE TABLE semantic_memories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cycle_id TEXT NOT NULL,
  character_id TEXT,                 -- NULL = 公共知识
  category TEXT NOT NULL,            -- 'fact' | 'rule' | 'person' | 'place' | 'lore' | 'rumor'
  subject TEXT,
  predicate TEXT,
  object_text TEXT,
  content TEXT NOT NULL,
  confidence REAL DEFAULT 1.0,
  source_event_id INTEGER,
  contradicting_event_ids TEXT,      -- JSON array
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT
);

-- F.3 情感记忆(emotional)
CREATE TABLE emotional_memories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cycle_id TEXT NOT NULL,
  character_id TEXT NOT NULL,
  target_id TEXT,                    -- 对谁的印象
  emotion_type TEXT NOT NULL,        -- 'love' | 'hate' | 'fear' | 'respect' | 'gratitude' | 'guilt' | 'longing'
  intensity REAL DEFAULT 0.5,        -- 0-1
  context TEXT,                      -- 触发原因
  trigger_event_id INTEGER,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  last_reinforced_at TEXT,
  reinforcement_count INTEGER DEFAULT 1
);

CREATE INDEX idx_emo_target ON emotional_memories(cycle_id, character_id, target_id);

-- ===== G. 物品栏(cycle 隔离) =====

CREATE TABLE items (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cycle_id TEXT NOT NULL,
  owner_id TEXT,                     -- npc_id 或 user_perspective_id 或 NULL(公共)
  item_name TEXT NOT NULL,
  item_type TEXT,                    -- 'weapon' | 'potion' | 'key' | 'armor' | 'book' | 'treasure' | 'food'
  quantity INTEGER DEFAULT 1,
  properties TEXT,                   -- JSON
  acquired_event_id INTEGER,
  acquired_at TEXT,
  acquired_from TEXT
);

CREATE INDEX idx_items_owner ON items(cycle_id, owner_id);

-- ===== H. 地点(cycle 隔离,已发现) =====

CREATE TABLE locations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cycle_id TEXT NOT NULL,
  location_name TEXT NOT NULL,
  parent_id INTEGER REFERENCES locations(id),
  location_type TEXT,                -- 'realm' | 'city' | 'town' | 'dungeon' | 'wilderness' | 'building' | 'room'
  description TEXT,
  properties TEXT,                   -- JSON
  discovered_event_id INTEGER,
  discovered_at TEXT
);

-- ===== I. 弧光维度定义(全局/world 级) =====

CREATE TABLE arc_dimensions (
  dimension_id TEXT PRIMARY KEY,     -- 'trust_user' | 'affection_user' | 'courage' | 'maturity' | ...
  dimension_name TEXT NOT NULL,
  category TEXT NOT NULL,            -- 'user_relationship' | 'personality' | 'skill' | 'moral'
  min_value REAL DEFAULT 0,
  max_value REAL DEFAULT 100,
  milestones TEXT,                   -- JSON array
  description TEXT,
  world_id TEXT                      -- NULL = 全局通用
);

-- 预置数据
INSERT INTO arc_dimensions(dimension_id, dimension_name, category, milestones) VALUES
  ('trust_user', '对 user 的信任', 'user_relationship',
   '[{"at":0,"label":"陌生人"},{"at":20,"label":"初识"},{"at":50,"label":"朋友"},{"at":80,"label":"生死之交"},{"at":100,"label":"以命相托"}]'),
  ('affection_user', '对 user 的爱慕', 'user_relationship',
   '[{"at":0,"label":"无感"},{"at":30,"label":"萌芽"},{"at":60,"label":"暗恋"},{"at":90,"label":"表白"}]'),
  ('hostility_user', '对 user 的敌意', 'user_relationship',
   '[{"at":0,"label":"无"},{"at":40,"label":"不满"},{"at":70,"label":"厌恶"},{"at":100,"label":"死敌"}]'),
  ('courage', '勇气', 'personality',
   '[{"at":0,"label":"怯懦"},{"at":30,"label":"平常"},{"at":60,"label":"勇敢"},{"at":100,"label":"无畏"}]'),
  ('maturity', '成熟度', 'personality',
   '[{"at":0,"label":"天真"},{"at":30,"label":"成长中"},{"at":60,"label":"成熟"},{"at":100,"label":"通透"}]');

-- ===== J. 全文检索(FTS5) =====

CREATE VIRTUAL TABLE events_fts USING fts5(
  summary, content='domain_events', content_rowid='id', tokenize='unicode61'
);

CREATE VIRTUAL TABLE episodic_fts USING fts5(
  content, content='episodic_memories', content_rowid='id', tokenize='unicode61'
);

CREATE VIRTUAL TABLE semantic_fts USING fts5(
  content, content='semantic_memories', content_rowid='id', tokenize='unicode61'
);

-- 触发器(以 events_fts 为例,其余类似)
CREATE TRIGGER events_ai AFTER INSERT ON domain_events BEGIN
  INSERT INTO events_fts(rowid, summary) VALUES (new.id, new.summary);
END;
CREATE TRIGGER events_ad AFTER DELETE ON domain_events BEGIN
  INSERT INTO events_fts(events_fts, rowid, summary) VALUES('delete', old.id, old.summary);
END;
CREATE TRIGGER events_au AFTER UPDATE ON domain_events BEGIN
  INSERT INTO events_fts(events_fts, rowid, summary) VALUES('delete', old.id, old.summary);
  INSERT INTO events_fts(rowid, summary) VALUES (new.id, new.summary);
END;
-- episodic_fts / semantic_fts 触发器类似,省略
```

### 2.3 Schema 关键设计决策

| 决策 | 原因 |
|---|---|
| **cycle 替代 save_slot** | 产品语义更清晰,玩家说"开新周目"自然 |
| **gm_id 是 file reference,不是表** | GM 就是一段 system_prompt 文件,不需要 first-class 表 |
| **character_states 不用 perspective_id** | cycle 已经隐含 perspective(perspective_id 是 cycle 字段) |
| **npc_user_relationships.cycle_id 隔离** | 同一 NPC 在不同 cycle 关系独立 |
| **setup event_type** | 标记 cycle 初始化的批量事件(用模板初始化关系时产生) |
| **initial_template 字段** | 记录 NPC 关系从哪个模板初始化,后续可审计 |

---

## 3. 实体关系图(ERD)

```
┌──────────────┐
│   worlds     │ (文件系统 + lorebook)
│  (1 个 World) │
└──────┬───────┘
       │ 包含
       ├─────────────────────────┐
       ▼                         ▼
┌──────────────┐          ┌──────────────┐
│  npc_base    │          │  gm_prompts  │
│ (性格不可变)  │          │  (.md 文件)   │
└──────┬───────┘          └──────┬───────┘
       │                         │
       │ 关联(可选)               │ 引用
       │                         │
┌──────┴──────────────────────────┴───────┐
│              cycles (周目)               │
│  (world + perspective + gm + 状态)      │
└──────┬──────────────────────────────────┘
       │ 1:N
       ├─────────────┬─────────────┬─────────────┐
       ▼             ▼             ▼             ▼
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│character │  │npc_user  │  │ domain   │  │  items   │
│ _states  │  │_relation │  │ _events  │  │locations │
│          │  │ _ships   │  │          │  │          │
└──────────┘  └────┬─────┘  └────┬─────┘  └──────────┘
                   │             │
                   │             ├── episodic_memories
                   │             ├── semantic_memories
                   │             ├── emotional_memories
                   │             └── relationship_history
                   ▼
           (N 个 cycle 完全隔离)


┌────────────────────────┐
│ user_perspectives      │ (跨 world 复用)
│ source='npc' | 'oc'    │
└──────┬─────────────────┘
       │ 引用(source='npc' 时)
       ▼
┌──────────────┐
│  npc_base    │ (内置 NPC 不可变)
└──────────────┘

┌────────────────────────┐
│ oc_relationship_       │ (OC 创建时引用)
│ templates              │
└────────────────────────┘
```

---

## 4. 完整流程

### 4.1 创建 OC + 开新周目(完整流程)

```
Step 1: 选 World
  用户在 WebUI 选"仙剑奇侠传一"
  → 加载 worlds/xiatian_qixia_1/setting.md + npcs/ + lorebook.json

Step 2: 创建 OC
  用户填表:
    - name: "师兄"
    - background: "天剑阁弟子,因师父遭人陷害而下山追凶..."
    - personality: "沉默寡言,剑术高超,内心柔软"
    - avatar: 上传图片

  系统展示内置 NPC 列表(从 npc_base 加载):
    □ 林月如
    □ 李逍遥
    □ 赵灵儿
    □ NPC_客栈老板

  用户对每个 NPC 选关系模板:
    林月如     →  [tpl_childhood_friend  青梅竹马]
    李逍遥     →  [tpl_stranger          萍水相逢]
    赵灵儿     →  [tpl_love_at_first_sight 一见钟情]
    NPC_客栈老板 → [tpl_stranger          萍水相逢]

  → 创建 perspective_id = 'pov_oc_shixiong'
  → 存 perspectives/pov_oc_shixiong.json

Step 3: 选 GM
  WebUI 展示 GM 列表(从 worlds/.../gms/ + gms/ 扫描):
    □ style_linyueru (林月如文风) - 仙剑世界专属
    □ style_gulong (古龙武侠) - 跨世界通用
    □ style_horror (恐怖) - 跨世界通用
    □ + 自定义 GM (上传 .md)

  用户选: "style_linyueru (林月如文风)"

Step 4: 选起始时间(可选)
  默认: worlds/.../rules.json 中的 start_time
  用户可改: "景天元年 三月初一 辰时"

Step 5: 输入周目名
  "周目 1: OC 师兄-仙剑奇侠传"

Step 6: 系统初始化(在 transaction 中)
  BEGIN TRANSACTION;

  -- 1. 创建 cycle
  INSERT INTO cycles(cycle_id, world_id, perspective_id, gm_id, cycle_name, current_world_time)
  VALUES('cycle_001', 'xiatian_qixia_1', 'pov_oc_shixiong', 'style_linyueru',
         '周目 1: OC 师兄', '景天元年 三月初一 辰时');

  -- 2. 加载 world 基础数据到 character_states
  INSERT INTO character_states(cycle_id, npc_id, arcs, hp, hp_max, mp, mp_max, location_id, arc_phase)
  SELECT 'cycle_001', npc_id,
         '{"trust_user": 0, "affection_user": 0, "courage": 70, ...}',  -- 默认 arcs
         100, 100, 50, 50,
         (SELECT default_location_id FROM world_npcs WHERE world_id='xiatian_qixia_1' AND npc_id=npc_base.npc_id),
         'stranger'
  FROM npc_base WHERE world_id='xiatian_qixia_1';

  -- 3. 按 OC 关系模板初始化 npc_user_relationships
  INSERT INTO npc_user_relationships(cycle_id, npc_id, affinity, trust, intimacy, respect, current_type, initial_template)
  VALUES
    ('cycle_001', 'lin_yueru', 60, 80, 70, 65, 'close_friend', 'tpl_childhood_friend'),
    ('cycle_001', 'li_xiaoyao', 0, 0, 0, 0, 'stranger', 'tpl_stranger'),
    ('cycle_001', 'zhao_linger', 70, 30, 50, 40, 'lover', 'tpl_love_at_first_sight'),
    ('cycle_001', 'npc_innkeeper', 0, 0, 0, 0, 'stranger', 'tpl_stranger');

  -- 4. 写 setup 事件(标记 cycle 初始化)
  INSERT INTO domain_events(cycle_id, event_type, summary, importance, payload)
  VALUES('cycle_001', 'setup',
         'OC 师兄 来到仙剑世界,初始关系已建立', 0.8,
         '{"perspective_id": "pov_oc_shixiong", "gm_id": "style_linyueru", "templates_applied": [...]}');

  COMMIT;

Step 7: 开始游戏
  WebUI 进入游戏主界面
  → GM 引擎加载 perspective/persona_data + world/setting + gm/content
  → 拼装 system_prompt
  → 等用户输入
```

### 4.2 单轮生成(完整流程)

```
Step 1: User 输入
  "我走进余杭镇,想找一家客栈休息"

Step 2: GM 引擎装配 context
  读取 cycle = 'cycle_001'
  加载 perspective.persona_data
  加载 gm.content
  加载 world.setting
  加载 current_state
  加载 relevant_memories (从 SQLite 查询)
  拼装 system_prompt (按 §1.4 拼接机制)

Step 3: 决定调哪些 NPC
  GM 分析:用户进入"余杭镇",需要 NPC_客栈老板
  → 触发 NPC_客栈老板 subagent

Step 4: NPC subagent 生成
  派生纯净 subagent context:
    - persona_data 来自 npc_base.npc_innkeeper (锁死)
    - 接收的感知: user 走进余杭镇
  subagent 输出:
    - visibleResponse: "客官里边请!本店上房干净,饭菜可口!"
    - privateIntent: "这人背剑独行,看着像江湖人,小心为上"
    - suggestedEvents:
      [{event_type: 'dialogue', actor: 'npc_innkeeper', summary: '客栈老板招呼 user'...}]

Step 5: GM 接收
  合并 subagent 输出到叙事

Step 6: 走 reducer 流水线
  event = DomainEvent {
    cycle_id: 'cycle_001',
    event_type: 'dialogue',
    actor_id: 'npc_innkeeper', actor_type: 'npc',
    target_id: 'pov_oc_shixiong', target_type: 'user',
    location_id: 'yuhang_town',
    importance: 0.3,
    summary: '客栈老板招呼 user'
  }
  reducer 写:
    - domain_events (1 行)
    - character_states WHERE npc='npc_innkeeper' (无变化,纯对话)
    - relationship_history (1 行,首次相遇)
    - npc_user_relationships (更新 first_met_at)
    - episodic_memories (NPC 自己 + user 各 1 行)

Step 7: 产出 patch
  [
    {op: 'add', path: '/events/-', value: event},
    {op: 'replace', path: '/npc_user_relationships/npc_innkeeper/first_met_at', value: '...'},
  ]

Step 8: WebUI 更新
  聊天区显示叙事
  NPC 列表高亮 npc_innkeeper
  关系图显示首次相遇提示
  → 用户继续
```

### 4.3 跨周目对比(高级功能)

```sql
-- 同一 NPC 在不同 cycle 的当前关系
SELECT
  c.cycle_id, c.cycle_name,
  p.perspective_name,
  r.affinity, r.trust, r.current_type,
  r.initial_template
FROM npc_user_relationships r
JOIN cycles c ON r.cycle_id = c.cycle_id
JOIN user_perspectives p ON c.perspective_id = p.perspective_id
WHERE r.npc_id = 'lin_yueru'
  AND c.world_id = 'xiatian_qixia_1'
ORDER BY c.created_at DESC;

-- 结果:
--   cycle_001 (师兄视角)  - 60/80/close_friend/青梅竹马
--   cycle_002 (李逍遥视角) - 90/95/lover/默认起点
--   cycle_003 (林月如视角) - 50/70/friend/默认起点
```

WebUI 展示"林月如在不同周目里对我的关系"对比表。

---

## 5. WebUI 设计

### 5.1 主导航

```
顶部 Tab:
  [世界]  [周目]  [NPC]  [OC]  [GM]  [设置]
```

### 5.2 关键页面

#### 世界选择页(首次进入)

```
┌────────────────────────────────────┐
│  仙剑 RP 平台                       │
├────────────────────────────────────┤
│  选一个世界开始:                     │
│  ┌──────┐ ┌──────┐ ┌──────┐         │
│  │仙剑1 │ │仙剑2 │ │自定义│         │
│  │  🏯  │ │  ⚔️  │ │  +  │         │
│  └──────┘ └──────┘ └──────┘         │
│  [进入世界]                          │
└────────────────────────────────────┘
```

#### 角色选择页(选内置 NPC 或创建 OC)

```
┌────────────────────────────────────┐
│  仙剑奇侠传一 / 选角色               │
├────────────────────────────────────┤
│  内置 NPC (性格不可变):              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐│
│  │李逍遥│ │林月如│ │赵灵儿│ │客栈  ││
│  │ 侠客 │ │  侠女 │ │  仙灵 │ │  老板 ││
│  └──────┘ └──────┘ └──────┘ └──────┘│
│                                      │
│  自定义 OC:                          │
│  ┌──────┐                            │
│  │  +   │ 创建新 OC                  │
│  │新建  │                            │
│  └──────┘                            │
└────────────────────────────────────┘
```

#### OC 创建向导(关键创新点)

```
┌────────────────────────────────────────────┐
│ 创建 OC: 步骤 2/3 - 设置初始关系             │
├────────────────────────────────────────────┤
│ 仙剑世界的 NPC:                              │
│                                              │
│ 林月如    [青梅竹马 ▾]                       │
│   └ 初始: 好感 60 / 信任 80 / 亲密 70        │
│                                              │
│ 李逍遥    [萍水相逢 ▾]                       │
│   └ 初始: 好感 0 / 信任 0 / 亲密 0           │
│                                              │
│ 赵灵儿    [一见钟情 ▾]                       │
│   └ 初始: 好感 70 / 信任 30 / 亲密 50        │
│                                              │
│ 客栈老板  [萍水相逢 ▾]                       │
│   └ 初始: 好感 0 / 信任 0 / 亲密 0           │
│                                              │
│ [+ 添加自定义 NPC 关系]                       │
│                                              │
│                  [上一步]  [下一步]            │
└────────────────────────────────────────────┘
```

#### GM 选择页

```
┌────────────────────────────────────┐
│ 选 GM (文风)                         │
├────────────────────────────────────┤
│ 仙剑世界自带:                        │
│ ⦿ 林月如文风 - 细腻情感              │
│ ○ 仙剑官方 - 经典剧情风              │
│                                      │
│ 跨世界通用:                          │
│ ○ 古龙武侠 - 短句凌厉                │
│ ○ 恐怖氛围 - 悬念压力                │
│ ○ 诙谐吐槽 - 现代幽默                │
│ ○ ...                               │
│                                      │
│ [+ 上传自定义 GM (.md 文件)]         │
│                                      │
│ [GM 预览]                            │
│ ┌──────────────────────────────┐    │
│ │ # 林月如文风                   │    │
│ │ 你是一位细腻情感向的叙事者...   │    │
│ │ ...                           │    │
│ └──────────────────────────────┘    │
└────────────────────────────────────┘
```

#### 周目列表页

```
┌────────────────────────────────────────────────────┐
│ 仙剑奇侠传一 / 周目                                  │
├────────────────────────────────────────────────────┤
│ 周目 3: 师兄-林月如文风    [2026-07-12]  3章        │
│  ├ 视角: OC 师兄                                   │
│  ├ GM: 林月如文风                                  │
│  └ 当前: 景天元年 三月初七 余杭镇                    │
│  [继续] [复制] [分享] [删除]                        │
│                                                     │
│ 周目 2: 林月如线-古龙    [2026-07-10]  5章        │
│  ├ 视角: 林月如 (内置)                              │
│  ├ GM: 古龙武侠                                    │
│  └ 当前: 景天元年 三月十五 锁妖塔                   │
│  [继续] [复制] [分享] [删除]                        │
│                                                     │
│ 周目 1: 李逍遥线-默认    [2026-07-08]  完结        │
│  ├ 视角: 李逍遥 (内置)                              │
│  ├ GM: 默认                                        │
│  └ 当前: 景天元年 五月初一 结局                     │
│  [继续] [复制] [分享] [删除]                        │
│                                                     │
│ [+ 开新周目]                                        │
└────────────────────────────────────────────────────┘
```

#### 游戏主界面(响应式 PC + 手机)

```
PC (≥1024px):
┌──────────┬───────────────────┬──────────────────┐
│ 周目信息  │  聊天主区          │  Widget 面板      │
│ + 角色列表│                   │                  │
│          │  NPC 客栈老板:     │  [State] 林月如   │
│ 林月如 70 │  客官里边请!      │   HP 100/100     │
│ 李逍遥 0  │                   │   信任 +80       │
│ 赵灵儿 70 │  你: 我来住店     │   爱慕 +60       │
│          │                   │                  │
│ [切 GM]  │  NPC 老板:        │  [Items] 物品栏   │
│ [切视角] │  天字号房 5 两银子 │   长剑 ×1        │
│          │                   │   令牌 ×1        │
│          │  [输入...]         │                  │
│          │                   │  [Affinities]     │
│          │                   │   林月如 ❤️ 60    │
│          │                   │   李逍遥  · 0    │
│          │                   │   赵灵儿 ❤️ 70    │
│          │                   │                  │
│          │                   │  [Arc Timeline]   │
│          │                   │   林月如 弧光     │
│          │                   │   ▰▰▰▰▰▱▱▱ 60%  │
└──────────┴───────────────────┴──────────────────┘

手机 (< 768px):
┌──────────────────────┐
│ 仙剑 RP  [≡] [角色]  │
├──────────────────────┤
│ 林月如 ❤️ 60  好感 60  │
│ 李逍遥 · 0  好感 0    │
│ 赵灵儿 ❤️ 70  好感 70  │
├──────────────────────┤
│ 客栈老板: 客官里边请!  │
│                      │
│ 你: 我来住店          │
│                      │
│ 客栈老板:             │
│ 天字号房 5 两银子      │
├──────────────────────┤
│ [输入消息...]          │
├──────────────────────┤
│ [聊天][状态][物品][我]│  ← 底部 tab
└──────────────────────┘
```

### 5.3 新增/调整 widget

| Widget | 来源 | 用途 |
|---|---|---|
| **WorldSelector** | 🆕 | 选世界 |
| **CycleList** | 🆕 | 周目列表(替代 SaveSlotSelector) |
| **OcCreator** | 🆕 | OC 创建向导(带关系模板选择) |
| **NpcSelector** | 🆕 | 选内置 NPC |
| **GmSwitcher** | 🆕 | 切换 GM(改 .md 文件即可) |
| **PerspectiveSwitcher** | 🆕 | 切换扮演视角 |
| **RelationshipMatrix** | 🆕 | N×M 关系矩阵 |
| **CycleCompare** | 🆕 | 跨周目对比 |
| ChatWidget | 已有 | 聊天 |
| StateWidget | 已有 | HP/MP/位置 |
| AffinityWidget | 已有 | 好感度 |
| InventoryWidget | 已有 | 物品栏 |
| ArcTimelineWidget | 已有 | 弧光时间线 |
| EmotionalMap | 已有 | 情感地图 |

---

## 6. 实施路径 M0-M11

| 阶段 | 时间 | 关键产出 |
|---|---|---|
| **M0** 基础验证 | 0.5 天 | engine + webui + MCP-Server 跑通 |
| **M1** 角色卡编译 | 0.5 天 | tavern2agent 编译仙剑内置 NPC |
| **M2** GM 编排器 | 2-3 天 | gm_router + reducers + prompt_assembler(GM 拼接) |
| **M3** 基础 widget | 2-3 天 | Chat/State/Affinity/Inventory/ArcTimeline |
| **M4** World/Cycle 数据模型 | 1 天 | SQLite schema + 文件系统布局 |
| **M5** NPC/OC/GM 数据层 | 1.5 天 | perspective + npc_base + gm_prompts 管理 |
| **M6** OC 关系模板 | 1 天 | oc_relationship_templates + 初始化流程 |
| **M7** GM Router 完整 | 2-3 天 | 角色 subagent + 5 重防护 + reducer 完整 |
| **M8** 跨周目对比 | 1 天 | SQL + widget |
| **M9** WebUI 完整 | 3-4 天 | 7 个新增 widget + 响应式 + PWA |
| **M10** 质量保证 | 2-3 天 | 串台/串关系/弧光跳变检测 + 审计 subagent |
| **M11** 仙剑完整 demo | 2 天 | 1 World + 4 NPC + 3 OC + 5 GM + 3 完整 Cycle |

**完整 MVP 预计 18-24 天**(单人)。

---

## 7. 与 1.md / 2.md 关系

| 文档 | 角色 | 现状 |
|---|---|---|
| **1.md** | 原始架构图(Mermaid) | ✅ 保留作为"理想视图"参考 |
| **2.md** | 技术能力详细文档 | ✅ 保留,大部分技术细节可复用 |
| **3.md** (本文) | **最终融合产品设计** | ✅ **唯一权威** |

**3.md 优先**: 新决策以 3.md 为准,1.md/2.md 作为背景。

**3.md 删除了 2.md 的哪些冗余**:
- ❌ user_perspectives 表 → 改为 perspectives/ 目录(JSON 文件)
- ❌ npc_npc_relationships 表 → 简化为 npc_base 内的 lore_reference
- ❌ game_masters 表 → **简化为 .md 文件**
- ❌ worlds 详细表 → 简化为文件系统目录
- ❌ save_slots → 改为 cycles(产品语义)

**3.md 新增**:
- ✅ World 作为顶层心智
- ✅ GM = .md 文件
- ✅ OC 关系模板系统
- ✅ 周目 (cycle) 概念
- ✅ source='npc'/'oc' 区分
- ✅ 完整的开周目流程
- ✅ 跨周目对比

---

## 8. 关键风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| GM 切换影响已生成的对话上下文 | 中 | 中 | GM 只影响新轮 system_prompt,不改已生成内容 |
| OC 关系模板过于简化(玩家想自定义初始值) | 中 | 中 | 提供"高级"模式,直接编辑 affinity/trust 等数值 |
| 跨周目共享数据时混淆 | 低 | 高 | cycle_id 强隔离,FOREIGN KEY 严格 |
| 内置 NPC 性格被 GM 改写 | 中 | 中 | npc_base 性格只注入角色 subagent context,GM system_prompt 在更高层,语义隔离 |
| FTS5 性能在大数据量下降 | 低 | 中 | WAL 模式 + 定期 VACUUM |
| 多 Cycle 备份占用空间 | 中 | 低 | cycle 数据可独立 .backup() |

---

## 9. 立即可做(按价值/工作量)

| # | 动作 | 时间 | 价值 |
|---|---|---|---|
| 1 | 写 `worlds/xiatian_qixia_1/` 仙剑世界数据 | 0.5 天 | 高(基础) |
| 2 | 写 5 个默认 GM .md 文件 | 0.5 小时 | 高(产品核心) |
| 3 | 写 SQLite schema v3 | 0.5 天 | 高(基础) |
| 4 | 写 OC 创建向导(纯前端 mock) | 0.5 天 | 高(产品创新点) |
| 5 | 写 prompt_assembler(GM 拼接核心) | 0.5 天 | **关键** |
| 6 | 写 reducer 流水线完整版 | 1 天 | 高 |
| 7 | 写 Cycle 列表/对比 widget | 1 天 | 中 |
| 8 | 写 RelationshipMatrix widget | 1 天 | 中 |

---

## 10. 一句话总结(终极版)

**3.md 把 1.md 的"GM 编排架构" + 2.md 的"事件溯源/记忆/弧光技术能力" + 你的"World/周目/GM/OC/关系模板"产品概念完全融合**:

- **World 是顶层主体**(选世界 → 选角色 → 选 GM → 开周目)
- **GM 是一段 system_prompt .md 文件**(文风是用户唯一常改的"风格层",不需 first-class 表)
- **OC 关系模板**在创建 OC 时为每个内置 NPC 选一个(快速开局,不等 LLM 自创)
- **周目(Cycle)**替代 save_slot(同世界可多次游玩,产品语义清晰)
- **source='npc'/'oc'** 明确区分(内置 NPC 性格锁死,OC 自由编辑)
- **事件溯源/三层记忆/弧光/5 重防护/响应式 webui** 全部沿用 2.md

**SillyTavern: 1 world × 1 chat × 1 user persona × N NPC 共享 prompt(串台)**
**3.md: N world × N cycle × N perspective(内置+OC) × N NPC × 隔离 subagent × 关系模板快速开局**

**SillyTavern 是单线程 chat 工具,3.md 是多世界 RP 沙盒平台。**

---

## 11. 技术栈选型(从 2.md 融合,适配本设计)

### 11.1 最终技术栈

本架构 = **5 个复用项目 + 1 个最薄胶水层**,全部不修改上游。

| 复用项目 | 角色 | 对应 3.md 组件 | 复用度 |
|---|---|---|---|
| **AIRP monorepo** (engine + protocol + webui + ui) | 后端 daemon + 浏览器 GUI + Tauri 桌面 + 协议 | 网关层 / 前端层 / GM 拼接 | 0 修改 |
| **AIRP-MCP-Server** | RP 数据后端 (38 工具) | 角色卡 / lorebook / chat.jsonl / volume / plugin 命名空间 | 0 修改 |
| **AIRP-State-Protocol** | Widget 协议 (Blueprint / RFC6902 patch / 同意闸门) | 状态栏 / 好感度 / 物品栏 / 弧光时间线 widget | 0 修改 |
| **tavern2agent** | 多 subagent 架构模板 (编译期产物) | 角色 subagent 模板 (林月如 / 李逍遥 / 赵灵儿) | 0 修改 |
| **SQLite (via rusqlite)** | 长期事实库 | character_states / npc_user_relationships / 3 层记忆 / FTS5 | 0 集成 |
| **(自实现) GM 编排器** | 胶水层 | World → Cycle → GM 拼接 → reducer 流水线 | ~3000 行 |

### 11.2 选型对比(为什么选这 5 个)

| 候选方案 | 是否入选 | 关键理由 |
|---|---|---|
| **AIRP monorepo engine** | ✅ | 唯一可用的 Rust HTTP agent loop (M_AGENT-1, 299 测试),戒律#6 已是代码级不变式 |
| **AIRP-MCP-Server** | ✅ | 唯一可用的 RP 数据后端 (38 工具),plugin 命名空间支持 cycle 元数据 |
| **AIRP-State-Protocol** | ✅ | 唯一可用的 widget 协议 (Blueprint + RFC6902 patch + 同意闸门) |
| **AIRP monorepo webui** | ✅ | 唯一可用的浏览器原生 GUI (零编译), 响应式 + PWA |
| **AIRP monorepo ui** | ✅ (可选) | 唯一可用的 Tauri+Vue 桌面 GUI |
| **tavern2agent** | ✅ (模板) | 唯一可用的多 subagent 架构模板, fate-sandbox 13 时间线已验证 |
| **SQLite via rusqlite** | ✅ | 嵌入、单文件、ACID、FTS5、JSON1、window functions,对单机 RP 足 99% 场景 |
| AIRP-Core | ❌ | 已被 monorepo engine 吸收,不在独立维护 |
| AIRP-Gateway | ⚠️ 可选 | HTTP↔MCP 桥, MCP HTTP 模式可省 |
| **Pi** | ❌ | 详见 §11.4 |
| AIRP monorepo airp-engine-console | ⚠️ 调研中 | 可能是独立 console 应用,待评估 |

### 11.3 选型理由详解

#### AIRP monorepo engine: agent loop 的不二之选

- 唯一已落地的 Rust agent loop (M_AGENT-1, /v1/agent/run)
- **299 passing tests** (297 lib + 2 integration)
- provider-native structured tool call (**不靠 prompt 解析**)
- 戒律#6 由 `subagent_context_has_no_orchestrator_noise` 测试守护
- 19 工具开箱即用
- **热重载 settings** (`POST /v1/settings` 不需重启)
- **Bearer 鉴权** + **QuotaConfig** (生产部署必备)

**在 3.md 中的角色**:
- 接受 /v1/chat/completions + /v1/agent/run 请求
- 装配 system_prompt = 角色人设 + GM .md + World setting + 记忆/弧光上下文
- 派生角色 subagent (perceived 隔离)
- 流式返回 SSE

#### AIRP-MCP-Server: 数据后端的不二之选

- 38 工具 / 19 资源 / 12 prompt 完整覆盖角色卡 / 世界书 / 会话 / 状态 / 记忆
- `plugin_kv` / `plugin_jsonl` / `plugin_blob` 命名空间支持自定义 cycle 元数据
- 路径安全锁在 data/ 内 (对自实现 cycle 友好)
- Rust 性能 + MIT/Apache 2.0 license

**在 3.md 中的角色**:
- 存 `worlds/xiatian_qixia_1/` 仙剑世界数据 (setting.md / npcs/ / gms/ / lorebook.json)
- 存 `perspectives/pov_*.json` 视角数据
- 存短期 `chat.jsonl` + 中期 `volume/*.md`
- 提供 L1/L2 记忆存储

#### AIRP-State-Protocol: widget 协议的不二之选

- 唯一支持 **Blueprint 模式**的 RP 协议
- Widget Registry **开放** (第三方可加 namespace.name widget)
- **RFC6902 patch** 增量同步 (低 Token)
- 同意闸门 + 沙箱隔离 (防恶意 widget)

**在 3.md 中的角色**:
- ChatWidget / StateWidget / AffinityWidget / InventoryWidget / ArcTimelineWidget
- 接收 cycle 状态的 patch 下行
- 渲染 Blueprint (声明式 UI)

#### tavern2agent: 多 subagent 模板的不二之选

- 唯一明确写出 **"subagent 不写 state"** 宪法的项目
- perspective / parallel-line / auditor 三种 subagent 现成
- **hiddenPublicPolicy** 现成
- fate-sandbox 13 时间线已生产验证

**在 3.md 中的角色**:
- 编译 ST 格式角色卡 → 生成 `.pi/agents/*.md` (林月如 / 李逍遥 / 赵灵儿)
- subagent 独立 context 隔离 (戒律#6 补充)
- hiddenPublicPolicy 保障 3 重不串台

#### SQLite via rusqlite: 长期事实库

- 嵌入式,单文件,与 MCP-Server 的文件哲学一致
- **ACID 事务** (跨表原子性)
- **FTS5 全文检索** (性能优于 ES, 零运维)
- **JSON1** + **window functions** (适配 LLM 工具入参)
- `sqlite-vec` 可选扩展 (语义检索)

**在 3.md 中的角色**:
- 存 `cycles` (周目) + `character_states` + `npc_user_relationships` + 3 层记忆
- 跨 cycle 共享/隔离由 `cycle_id` 显式控制
- 弧光阶段自动判定
- 记忆衰减 (vividness × importance × access_count)

### 11.4 为什么不用 Pi (从 2.md 提炼)

| 维度 | Pi | AIRP engine | 对 3.md 的影响 |
|---|---|---|---|
| **GUI** | TUI only | **webui + Tauri 桌面双形态** | Pi 缺 widget 渲染(本架构产品核心) |
| **MCP 支持** | ❌ 无 | ✅ AIRP-MCP-Server | Pi 要 wrap 38 工具(额外 ~600 行 ts) |
| **Widget 协议** | ❌ 无 | ✅ State Protocol | Pi widget 要自实现 |
| **多端共享** | ❌ 本地进程 | ✅ HTTP server | Pi 移动端/共享要自建 |
| **多 World × 多 Cycle** | ⚠️ 不友好 | ✅ settings 多 profile | Pi session tree 不适合世界维度 |
| **Provider** | 4+ (Anthropic/OpenAI/Google/自定义) | 2 (OpenAI/Anthropic) | Pi 多,但 OpenAI+Anthropic 足 |
| **测试覆盖** | 未披露完整 | 299 passing | AIRP 强 |
| **集成复杂度** | 需 wrap 38 工具 | **0** | AIRP 完胜 |

**唯一借鉴 Pi 的点**: detached 密闭导演型 subagent (OS 进程级隔离)。tavern2agent 已提供该模式 (默认 in-process, 可按需启用 detached)。

**结论**: Pi 的优势 (subagent 进程级隔离) 对 3.md 不足以抵消其他 8 个维度的劣势。**AIRP 完胜**。

### 11.5 各项目对应到 3.md 组件(完整映射)

```
3.md 组件                    实现来源
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① World (世界)              AIRP-MCP-Server 文件系统
                              worlds/xiatian_qixia_1/{setting.md,npcs/,gms/,lorebook.json,...}

② NPC (内置,不可变)         AIRP-MCP-Server 文件系统
                              worlds/.../npcs/*.md
                              (GM 引擎读取时锁死 persona_data)

③ OC (自定义)               AIRP-MCP-Server 文件系统
                              perspectives/pov_oc_*.json
                              (用户可自由编辑 persona_data)

④ GM (文风 system_prompt)   AIRP-MCP-Server 文件系统
                              gms/*.md  或  worlds/.../gms/*.md
                              (GM 引擎的 prompt_assembler 拼接)

⑤ OC 关系模板               SQLite (oc_relationship_templates 表)
                              (开 cycle 时批量初始化 npc_user_relationships)

⑥ Cycle (周目)              SQLite (cycles 表)
                              + AIRP-MCP-Server session 路径
                              (cycle_id 隔离)

⑦ NPC↔User 关系             SQLite (npc_user_relationships 表)
                              (按模板初始化,后续由 reducer 维护)

⑧ Character States          SQLite (character_states 表)
                              (per-cycle 状态投影)

⑨ Domain Events             SQLite (domain_events 表)
                              (事件溯源单一真相源)

⑩ 3 层记忆                  SQLite (episodic/semantic/emotional_memories 表)
                              + FTS5 (events_fts, episodic_fts, semantic_fts)

⑪ GM 拼接核心               GM 编排器 engine/orchestrator/prompt_assembler.rs
                              (按 §1.4 拼接机制组装 system_prompt)

⑫ Reducer 流水线            GM 编排器 engine/reducers/*.rs
                              (event → state 投影 + patch 生成)

⑬ 角色 subagent 隔离        tavern2agent 模板
                              (.pi/agents/lin-yueru.md 等)
                              + AIRP engine 戒律#6 (subagent_context_has_no_orchestrator_noise)

⑭ 5 重防护                  GM 编排器 + AIRP engine + tavern2agent
                              (独立 context / hiddenPublicPolicy / tools 白名单 /
                              event 归口 / auditor)

⑮ WebUI 渲染                AIRP monorepo webui (浏览器) + ui (Tauri 桌面)
                              + AIRP-State-Protocol Widget Registry

⑯ 7+ widget                 AIRP-State-Protocol 扩展
                              (Chat / State / Affinity / Inventory / ArcTimeline /
                              EmotionalMap / RelationshipMatrix / SaveSlotSelector)
```

### 11.6 复用层与自实现层边界

#### 复用(0 修改,占架构 80%)

| 组件 | 来源 | 体积 |
|---|---|---|
| HTTP daemon + agent loop | AIRP monorepo engine | 0 自写 |
| 数据后端 (38 工具) | AIRP-MCP-Server | 0 自写 |
| Widget 协议 + Registry | AIRP-State-Protocol | 0 自写 |
| 浏览器 GUI 框架 | AIRP monorepo webui | 0 自写 (只扩展 widget) |
| Tauri 桌面框架 | AIRP monorepo ui | 0 自写 (只扩展 widget) |
| 多 subagent 模板 | tavern2agent (代码生成产物) | 0 自写 (只跑) |
| LLM provider | OpenAI 兼容 + Anthropic | 0 自写 |

#### 自实现(胶水层,最薄,占架构 20%)

| 文件 | 行数 | 用途 |
|---|---|---|
| `engine/orchestrator/prompt_assembler.rs` | ~400 | GM 拼接核心 (按 §1.4 机制) |
| `engine/orchestrator/gm_router.rs` | ~500 | 角色调度 + 感知注入 + reducer 派发 |
| `engine/reducers/arc_reducer.rs` | ~200 | arc_increment 事件 → state 投影 |
| `engine/reducers/relationship_reducer.rs` | ~250 | 关系变更事件 → relationships + history |
| `engine/reducers/memory_reducer.rs` | ~250 | event → 3 层记忆 |
| `engine/reducers/state_reducer.rs` | ~200 | HP/MP/位置/状态变化 |
| `engine/events/domain_event.rs` | ~300 | TypedEvent 定义 + schema |
| `engine/cycle/cycle_manager.rs` | ~300 | cycle 创建/加载/切换/复制 |
| `engine/cycle/oc_initializer.rs` | ~250 | OC 关系模板 → 初始化 npc_user_relationships |
| `db/migrations/0001_init.sql` | ~300 | §2.2 完整 schema |
| `db/queries/*.rs` | ~600 | 5 类预编译查询 |
| `webui/widgets/WorldSelector.vue` | ~150 | 世界选择 |
| `webui/widgets/CycleList.vue` | ~250 | 周目列表 + 对比 |
| `webui/widgets/OcCreator.vue` | ~400 | OC 创建向导 (3 步式) |
| `webui/widgets/NpcSelector.vue` | ~150 | 内置 NPC 选择 |
| `webui/widgets/GmSwitcher.vue` | ~200 | GM 切换 |
| `webui/widgets/PerspectiveSwitcher.vue` | ~200 | 视角切换 |
| `webui/widgets/RelationshipMatrix.vue` | ~250 | N×M 关系矩阵 |
| `webui/widgets/CycleCompare.vue` | ~250 | 跨周目对比 |
| 现有 6 个 widget (Chat/State/Affinity/Inventory/ArcTimeline/EmotionalMap) | ~900 | 沿用 2.md §14.4 |
| 响应式 CSS + PWA manifest | ~200 | PC + 手机适配 |
| GM .md 模板 (仙剑世界) | ~5 文件 | 林月如文风 / 古龙 / 诙谐 / 等等 |

**总计自实现代码**: **~6000 行** (含 widget, 是 2.md 估的 3000 行的 2 倍, 因为加了 World/Cycle/OC 三个产品层)
**复用代码**: **0 修改**

### 11.7 与 2.md 技术栈章节的关系

本节是 2.md §2 的**适配版**,变更:

| 变更 | 原因 |
|---|---|
| 加 "对应 3.md 组件" 映射 | 让技术选型直接映射到 3.md 的产品概念 |
| 加 "自实现层边界" 详细表 | 2.md §4 估的 ~3000 行偏小, 实际 6000 行 (含 widget) |
| 加 "为什么不选 Pi" 单独小节 | 2.md §6 已写, 这里收敛成决策记录 |
| 加 "airp-engine-console" 备注 | 2.md 标"调研中", 仍待评估 |
| 加 "项目对应表" §11.5 | 3.md 独有, 把产品概念和技术实现一一对应 |

**2.md §2 仍可独立阅读**: 2.md 是"技术能力文档", §2 偏技术评估; 3.md §11 偏"产品视角下的技术选型", 互为补充。

### 11.8 立即可做(技术栈相关)

| # | 动作 | 时间 | 价值 |
|---|---|---|---|
| 1 | 验证 AIRP monorepo 全部子项目可拉取/编译 | 0.5 天 | 高(基础) |
| 2 | 拉取 AIRP-MCP-Server + AIRP-State-Protocol,确认 API 兼容 | 0.5 天 | 高 |
| 3 | 安装 tavern2agent skill,编译 1 个测试角色卡 | 0.5 小时 | 高 |
| 4 | 写 `db/migrations/0001_init.sql` v3 (本架构 schema) | 0.5 天 | 高 |
| 5 | 写 `engine/orchestrator/prompt_assembler.rs` (GM 拼接) | 0.5 天 | **关键** |
| 6 | 写 5 个 reducer 完整版 | 1.5 天 | 高 |
| 7 | 写 OC 关系模板 + cycle 初始化逻辑 | 1 天 | **产品创新** |
| 8 | webui 扩展 7 个新 widget | 3 天 | 高 |

---

## 12. 最终三文档关系

| 文档 | 角色 | 状态 |
|---|---|---|
| **1.md** | 原始架构图 (Mermaid) | ✅ 保留, "理想视图" |
| **2.md** | 技术能力详细文档 | ✅ 保留, "技术背景" |
| **3.md** (本文) | **最终融合产品设计** | ✅ **唯一权威, 含技术栈选型** |

**3.md 优先**: 新决策以 3.md 为准, 1.md/2.md 作为历史参考。

**3.md §11 = 2.md §2 的产品视角重述**: 看技术选型理由看 2.md §2, 看产品对应看 3.md §11。
